import 'package:flutter/material.dart';

class TableSreen extends StatelessWidget {
  const TableSreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('This is my widget!'),
    );
  }
}
